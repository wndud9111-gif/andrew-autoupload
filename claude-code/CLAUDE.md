# 프로젝트 안내

블로그·인스타그램 초안 준비나 재개 요청은 `andrew-autoupload/SKILL.md`를 읽고 수행한다.

브라우저 경로는 SKILL.md의 ‘브라우저 경로 선택’이 정한다. 기본은 Chrome이고, 거기서 정한 조건을 만족할 때만 ego lite를 쓴다. 어느 경로에서든 OS 화면 제어·네이티브 파일 선택창은 사용하지 않는다.

최종 공개 게시·발행·공유는 누르지 않고 사용자가 검수하도록 보고한다.
